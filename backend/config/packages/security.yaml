security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: "auto"

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    # Definimos de dónde vienen los usuarios
    providers:
        app_user_provider:
            entity:
                class: App\Auth\Domain\Entity\User
                property: email

    firewalls:
        # Ensure dev tools and static assets are always allowed
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false

        # Cortafuegos principal de la API
        api:
            pattern: ^/api
            stateless: true
            jwt: ~ # Activa el bundle de LexikJWT para validar el token

        # main:
        #     lazy: true
        #     provider: app_user_provider

            # Activate different ways to authenticate:
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Note: Only the *first* matching rule is applied
    access_control:
        # Principio de Mínimo Privilegio y la Prevención de Errores Futuros (Seguridad por Defecto).

        # Endpoints públicos explícitos de autenticación
        - { path: ^/api/auth/login$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/auth/refresh, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/auth/logout, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/auth/register$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/auth/verify-code$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/auth/login-code$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/auth/login-code/verify$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/auth/confirm/[^/]+$, roles: PUBLIC_ACCESS, methods: [GET] }

        # Documentación de la API
        - { path: ^/api/doc, roles: PUBLIC_ACCESS }

        # Cierre por defecto: El resto de la API requiere autenticación
        - { path: ^/api, roles: IS_AUTHENTICATED_FULLY }

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
